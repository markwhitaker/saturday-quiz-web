{
  "type": "module",
  "module": "es6",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "test": "npm run build && mocha dist/**/*.js",
    "test:js": "mocha test/**/*.js"
  },
  "devDependencies": {
    "@types/mocha": "^11.0.5",
    "@types/node": "^24.0.13",
    "mocha": "^11.6.0",
    "typescript": "^5.8.3"
  }
}
